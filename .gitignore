# Gradle / build output
.gradle/
build/
*/build/
.kotlin/

# Never commit: Picovoice AccessKey lives here (see README)
local.properties

# Wake models are large binary artifacts, downloaded not committed.
# Vosk en-IN model (~36MB): see README for the download step.
app/src/main/assets/model-en-in/
# (legacy) Porcupine custom keyword, per-account artifact.
app/src/main/assets/keywords/*.ppn

# IDE
.idea/
*.iml
.DS_Store
__pycache__/
